from libqtile import widget
from libqtile import qtile

widget_defaults = dict(
        font='Input Mono', # 'SF Mono'
        fontsize=20,
        padding=8
)
extension_defaults = widget_defaults.copy()